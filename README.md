# peersim-djl

Run instructions

## Backend (Spring Boot)

From the project root, start the backend with:

```powershell
mvn spring-boot:run
```

## Frontend (UI)

In a separate terminal, run:

```powershell
cd ./ui
npm install
npm run dev
```

That's it — the backend will run via Spring Boot and the frontend via the local dev server.

